package be.cytomine.appengine.dto.inputs.task;

public enum ParameterType { // add types to this enum
    BOOLEAN,
    INTEGER,
    NUMBER,
    STRING,
    ENUMERATION,
    DATETIME,
    GEOMETRY,
    IMAGE,
    FILE,
    ARRAY
}
