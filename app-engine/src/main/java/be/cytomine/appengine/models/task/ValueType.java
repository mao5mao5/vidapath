package be.cytomine.appengine.models.task;

public enum ValueType {
    BOOLEAN,
    INTEGER,
    NUMBER,
    STRING,
    ENUMERATION,
    DATETIME,
    GEOMETRY,
    IMAGE,
    FILE,
    ARRAY,
    REFERENCE
}
